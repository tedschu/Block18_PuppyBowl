// Lists potential testing requirements:

// Questions / features to test:
    // All players are displayed on main page
    // "See details" view displays: (1) player's name, breed, team, (2) enlarged picture, (3) return to main list button, (4) remove button
        // Does the "remove" button take the player off of the roster?
    // Does the "add new player" form create a new player?
        // Does it include (1) player name, (2) breed, (3) submit button
